import * as actions from 'redux-router';

export const push = actions.push;
export const replace = actions.replace;
export const go = actions.go;
export const goBack = actions.goBack;
export const goForward = actions.goForward;
